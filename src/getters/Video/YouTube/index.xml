index:	0
title:	YouTube
desc:	[V:YouTube] <a href="http://www.youtube.com/">"http://www.youtube.com/"</a>

body:	
	<p>Status: OK 2010-04-03</p>
	<pre># $Id: YouTube 11297 2010-04-03 00:32:29Z sparky $
# Video::YouTube - Video getter plugin for rsget.pl
#
# 2009-2010 (c) Przemysław Iskra &lt;sparky@pld-linux.org&gt;
#		This program is free software,
# you may distribute it under GPL v2 or newer.

name: YouTube
short: V:YouTube
web: "http://www.youtube.com/"
uri: qr{youtube\.com/watch\?v=.*}
slots: max
status: OK 2010-04-03

unify:
	s/#.*//;
	m{[\?&amp;]v=([-_a-zA-Z0-9]+)(?:&amp;.*?)?$};
	return "http://www.youtube.com/watch?v=$1" if $1;

pre:
	my @fmt = (
		[ 37, "mp4", "1080p" ],
		[ 22, "mp4", "720p" ],
		[ 35, "flv", "HQ" ],
		[ 18, "mp4", "LQ" ],
	);

start:
	GET( $-{_uri} );

	! m{"video_id": "(.*?)"} or m{'VIDEO_ID': '(.*?)'};
	my $id = $1;

	! m{&lt;meta name="title" content="(.*?)"&gt;};
	my $name = de_ml( de_ml( $1 ) );
	$name =~ s{/}{_}g;

	! /"fmt_map"\s*:\s*"(.*?)"/ or m{&amp;fmt_map=(\S+?)&amp;};
	my %fmts = map { m{(\d+)/}; $1 =&gt; $_ } split ",", uri_unescape( $1 );

	my $fmt = $fmt[ $#fmt ];
	foreach my $f ( @fmt ) {
		if ( exists $fmts{ $f-&gt;[0] } ) {
			$fmt = $f;
			last;
		}
	}

	! /"t"\s*:\s*"(.*?)"/ or m{&amp;t=(\S+?)&amp;};
	my $t = $1;

	my $fname = $name . "." . $fmt-&gt;[1];
	INFO( name =&gt; $fname, fmt =&gt; $fmt-&gt;[0], quality =&gt; $fmt-&gt;[2] );

	DOWNLOAD( "http://youtube.com/get_video?video_id=$id&amp;t=$t&amp;fmt=$fmt-&gt;[0]",
		fname =&gt; $fname );

# vim: filetype=perl:ts=4:sw=4
</pre>
